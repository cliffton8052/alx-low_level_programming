#include <stdio.h>
/**
 * main - A program of code that uses prinf to output a command
 * Return: 0 (Pass)
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
