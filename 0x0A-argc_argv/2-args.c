#include <stdio.h>

/**
 * main - prints arguments passed in the program
 * @argc: int
 * @argv: list int value
 * Return: 0
 */

int main(int argc, char const *argv[])
{
	int in = 0;

	while (argc--)
	{
		printf("%s\n",  argv)[i];
		i++;
	}

	return (0);
}
